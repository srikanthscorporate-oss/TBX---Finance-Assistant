services:
  frontend:
    container_name: strawhat-frontend-prod

    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL}

    environment:
      NODE_ENV: production

    ports:
      - "3000:3000"